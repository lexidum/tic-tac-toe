let board = [
  [null, null, null],
  [null, null, null],
  [null, null, null],
];
function makeMove(board, player, x, y) {
  return board;
}
function showBoard(board) {
  console.log(board);
}
showBoard(board);
board = makeMove(board, "A", 2, 2);
showBoard(board);
